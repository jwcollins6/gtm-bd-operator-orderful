# Per-client configuration. Never put credentials here directly — reference an env var name instead.
#
# STATUS: demo, not a live client -- this isn't a pitch to run BD for
# Orderful. Fields below are either public fact (sourced, see
# onboarding/company-understanding-inventory.md) or explicitly TBD since
# there's no client relationship. Nothing here is fabricated to look more
# complete than it is.

client:
  name: "Orderful"
  contact: "Mike Head, CRO — intended audience for this demo (source: orderful.com/about/our-story), not a point of contact for a client engagement."
  industry: "Modern EDI / B2B supply-chain integration platform (B2B SaaS)"

operating_model:
  operated_by: "TBD"     # n/a — no client relationship
  training_status: "n/a — demo only, no client relationship"

data_platform:
  track: "TBD"            # Track A vs B unknown — depends on Orderful's own CRM/tooling, not researchable publicly. See onboarding/tech-stack-audit-template.md (blank).
  storage: "TBD"
  notes: "Orderful is itself an integration/API platform, which may make Track A (live-connected) more viable than for a typical client — but that's a guess, not a finding. Needs a real tech-stack audit."

engagement_note: "No client engagement -- this instance was built 2026-09-17 as a demo to show Orderful's CRO (Mike Head) a system I built, not a pitch to run BD for Orderful."

autonomy:
  outbound_send: "draft_only"   # default per docs/eval/eval-loop.md — not yet a real decision for this client
  inbound_reply: "draft_only"

eval:
  feedback_mode: "manual"
  spot_check_cadence: "TBD"
